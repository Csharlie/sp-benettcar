node_modules
dist
*.tsbuildinfo

# === Runtime ===
# Assembled WP runtimes — NEVER committed
.local/

# === Environment ===
# .env committed (defaults), .env.local gitignored (overrides)
.env.local
.env.*.local
