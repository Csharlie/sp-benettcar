VITE_DATA_SOURCE=wordpress
VITE_WP_API_BASE=http://benettcar.local
VITE_DEV_ORIGIN=http://localhost:5174
SPEKTRA_CLIENT_CONFIG=D:\Projects\spektra\sp-clients\sp-benettcar\infra\config.php

# Contact form driver — `noop` in dev (console.log + fake success), `cf7` in prod
# (see .env.production). Override locally in .env.local if testing cf7 against
# wp.benettcar.hu.
VITE_FORM_HANDLER=noop
